# rentwala.vip frontend

## Setup

1. Copy `.env.example` to `.env.local` and configure values.
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start app:
   ```bash
   npm run dev
   ```

## Deploy

- Deploy to Vercel.
- Set env vars from `.env.example`.
